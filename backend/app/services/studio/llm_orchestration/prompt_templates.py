"""三个编排服务使用的提示词模板。

来源：中控台 ``prompts.py`` / ``llm_client.py``。迁移原则：
- 保留"只输出合法 JSON 对象，不要 Markdown"这类硬约束；
- 保留分层结构与质量规则（主体/动作/环境/镜头/风格/画质、负面词、时长档位）；
- 模板变量用 ``$name`` 占位（``string.Template``），避免 JSON 示例里的大括号被
  ``str.format`` 误解析——这是从源头减少"模板渲染炸掉"的一处刻意取舍。

模板内容不落库、不改数据库结构：它们只是本次编排服务的内部实现细节。
"""

from __future__ import annotations

from string import Template

# ---------------------------------------------------------------------------
# 4.1 实体提取
# ---------------------------------------------------------------------------

ENTITY_EXTRACTION_TEMPLATE = Template(
    """
你是 AI 短剧生产系统的实体提取助手。你的任务是从剧本/章节原文中提取**可复用的资产实体**草稿。

## 硬性要求

- 只返回合法 JSON 对象，不要 Markdown，不要解释，不要代码围栏。
- 只提取原文中**真实出现**的实体，禁止编造原文没有出现的人名、地点、物件。
- 实体类型只能是以下三者之一：character（人物/角色）、scene（场景/地点）、prop（道具/关键物件）。
- 服装、妆容、音效、BGM 不属于本次提取范围，不要返回。
- profile 用一句话画像，写可在生图/生视频时复用的视觉识别点（身份、年龄感、气质、外观特征、
  空间结构、材质），不要写剧情评价。
- confidence 是 0~1 的小数，表示"这个实体确实应作为独立资产"的把握；拿不准就给低分。

## 输出 JSON 结构

{
  "entities": [
    {
      "name": "实体标准名",
      "aliases": ["原文中出现的其他叫法"],
      "entity_type": "character | scene | prop",
      "profile": "一句话画像",
      "confidence": 0.0
    }
  ]
}

## 规则

- name 用原文中最常用的完整称呼，不要加括号补充。
- aliases 只放原文里确实出现过的别名/简称/职称；没有就给空数组。
- 同一个人物不要拆成多条；把别名合并进 aliases。
- 最多返回 $max_items 条；优先返回剧情功能明确、会反复出现的实体。
- 原文信息不足时，profile 写"信息不足，需人工补充"，不要编造细节。

## 候选约束

$candidate_names

## 附加要求

$extra_instructions

## 章节：$chapter_id

剧本原文：
$chapter_text
""".strip()
)


# ---------------------------------------------------------------------------
# 4.1b 章节结构化资产清单（第 2 步「资产准备」的数据源）
# ---------------------------------------------------------------------------

CHAPTER_ASSET_PROFILE_TEMPLATE = Template(
    """
你是 AI 短剧生产系统的**整集资产分析师**。你的任务是基于**本章完整剧本**与**本章分镜**，
产出这一集要建的**角色 / 场景 / 道具 / 服装**清单，并为每一项整理出**可直接用于出图**的
结构化资料。

## 硬性要求

- 只返回合法 JSON 对象，不要 Markdown，不要解释，不要代码围栏。
- **只能依据本章剧本原文与分镜内容**，禁止编造原文里没有的人、地点、物件，禁止添加原文
  没有的剧情。原文没写的信息，字段留空字符串 ""，**不要**写「不详」「未知」「需人工补充」
  这类空话——留空就是留空。
- 资产类型只能是：character（角色/人物）、scene（场景/地点）、prop（道具/关键物件）、
  costume（服装/造型）。**服装必须单独作为 costume 返回**（不要只塞进角色的服装配饰里）。
- 已经给出的「本章已有提取候选」是分镜阶段提取的候选清单：**优先使用这些名称**，
  同一实体会出现多种叫法时，把标准名填进 name、其它叫法填进 aliases，
  不要为同一个人/物/地点造两个条目。
- 每个字段写**具体的视觉事实**（材质、颜色、形状、款式、空间结构、光线……），
  不要写剧情评价（「她很可怜」）或情绪概括（「气氛压抑」要落到光线/色调/陈设上）。
- fields 里的每个值都用中文短句，句内可用顿号分隔多个特征；不要写成整段作文。
- shot_indexes 填该资产实际出现的**镜头序号**（数字，来自下面的分镜列表）；
  无法确定时给空数组，不要猜。
- evidence 填**本章剧本里逐字出现过的原文片段**（可以直接从剧本复制）。
  不要改写、不要拼接。
- 最多返回 $max_items 条；优先返回剧情功能明确、会反复出现的资产。

## fields 字段表（严格按这些 key 输出，不要自创 key）

$field_schemas

## 输出 JSON 结构

{
  "assets": [
    {
      "name": "标准名（用原文最常用的完整称呼）",
      "aliases": ["原文里出现过的其它叫法"],
      "asset_type": "character | scene | prop | costume",
      "fields": { "该类型的字段 key": "具体内容" },
      "shot_indexes": [1, 3],
      "evidence": ["原文片段"]
    }
  ]
}

## 本章已有提取候选（优先复用这些名称）

$candidate_list

## 本章分镜（含镜头 id 与序号）

$shot_list

## 附加要求

$extra_instructions

## 章节：$chapter_title

剧本原文：
$chapter_text
""".strip()
)


# ---------------------------------------------------------------------------
# 4.2 图片提示词生成
# ---------------------------------------------------------------------------
IMAGE_PROMPT_TEMPLATE = Template(
    """
你是 AI 短剧生产系统的图片提示词助手。你的任务是为一个镜头涉及的各图片槽位生成可直接提交
给图片模型的提示词。

## 硬性要求

- 只返回合法 JSON 对象，不要 Markdown，不要解释，不要代码围栏。
- 每个槽位的提示词必须严格按分层结构书写，顺序为：
  主体描述 + 动作姿态 + 场景环境 + 镜头语言（景别/角度/光线）+ 风格 + 画质词。
- **同一实体的主体描述必须与下方画像卡完全一致**：画像卡怎么写，subject 就怎么填，
  不要自己改写主体外观。跨槽位要保持同一张脸、同一套服装、同一处空间。
- 只写画面内容本身，禁止写入本地路径、outputs/assets、asset://、http(s):// 等地址。
- 不要写"参考图1""主体1""@图片1"，参考图由系统单独传入。
- 画面里不要出现任何文字、字幕、水印、LOGO、UI。

## 输出 JSON 结构

{
  "slots": [
    {
      "category": "槽位类别代码",
      "subject": "主体描述",
      "action_pose": "动作姿态",
      "environment": "场景环境",
      "camera_language": "镜头语言（景别/角度/光线）",
      "style": "风格",
      "quality": "画质词",
      "entity_name": "该槽位对应的实体名；帧类槽位可留空"
    }
  ]
}

只返回上面列出的 category，不要新增类别，不要遗漏。若某个槽位确实无法生成，仍然返回该槽位
并把字段写成空字符串。

## 需要生成的槽位

$slot_list

## 实体画像卡（必须严格遵守）

$entity_profiles

## 镜头文本

$shot_text

## 项目与风格上下文

$project_context

全局风格提示：$style_hint
全局负面提示词：$negative_prompt
""".strip()
)


# ---------------------------------------------------------------------------
# 4.3 视频提示词生成
# ---------------------------------------------------------------------------

VIDEO_PROMPT_TEMPLATE = Template(
    """
你是 AI 短剧生产系统的视频提示词助手。你的任务是把镜头文本整理为一条可直接提交给视频模型
的视频提示词。

## 硬性要求

- 只返回合法 JSON 对象，不要 Markdown，不要解释，不要代码围栏。
- final_prompt 只写视频内容本身：主体、动作、运镜、场景、表情氛围、真人短剧风格。
- 禁止写入本地路径、outputs/assets、asset://、http(s):// 等地址。
- 不要写"参考图1""主体1""@图片1"，首帧图由系统单独传入。
- 画面里不要出现任何文字、字幕、水印、LOGO、UI。
- 不要添加原文没有的新剧情。

## 运镜词约束（重要）

运镜只能从下面的标准词库里选一个，把它的 key 填进 camera_movement 字段：
$camera_options

**不要自己发明运镜词。** 如果镜头文本没有指定运镜，请根据内容选择一个最合适的标准词。

## 时长约束

- duration_seconds 只能取以下值之一：$allowed_durations
- 参考建议时长：$duration_hint

## 输出 JSON 结构

{
  "camera_movement": "标准运镜词 key",
  "camera_shot": "景别（远景/全景/中景/近景/特写/大特写/过肩）",
  "angle": "机位角度（平视/仰拍/俯拍/低角度）",
  "duration_seconds": 5,
  "action_beats": ["按时间顺序的主体动作要点"],
  "subject_action": "主体动作描述（谁、做什么、动作起点到结果）",
  "expression_mood": "表情与氛围",
  "atmosphere": "环境氛围与光影",
  "first_frame_handling": "首帧如何衔接（首帧图里已经有什么，视频从哪里开始动）",
  "last_frame_handling": "尾帧如何收束（若为单帧模式，写空字符串）",
  "final_prompt": "可直接提交的视频提示词，包含主体动作 + 运镜 + 表情氛围 + 时长",
  "negative_prompt": "负面提示词"
}

## 动作细节规则

用身体细节代替抽象情绪，不要写"她很害怕""气氛很压抑"。
要写：双手颤抖向前伸出、喉结上下滚动、指节因用力泛白、膝盖突然弯折。
动作要有起点 → 过程 → 结果。

## 帧模式

当前帧模式：$frame_mode
- single_frame：只有首帧图，动作从首帧自然延续；last_frame_handling 留空。
- first_last_frame：同时有首帧与尾帧图，动作必须在两者之间连续完成，不能跳跃。

## 实体画像卡（用于保持一致性）

$entity_profiles

## 镜头文本

$shot_text

## 首尾帧引用

首帧图引用：$first_frame_ref
尾帧图引用：$last_frame_ref

## 项目与风格上下文

$project_context

全局风格提示：$style_hint
全局负面提示词：$negative_prompt
""".strip()
)


# ---------------------------------------------------------------------------
# 通用系统提示词
# ---------------------------------------------------------------------------

JSON_ONLY_SYSTEM_PROMPT = (
    "你必须严格按用户要求输出，只返回合法 JSON 对象，不要添加解释、Markdown 或代码围栏。"
)


# ---------------------------------------------------------------------------
# P2 资产绑定（LLM 实体链接）
# ---------------------------------------------------------------------------
# 来源：用户方案文档 LLM_ASSET_BINDING_PLAN.md §4，按 Jellyfish 需求补了「服装」槽位
# （该文档原文只有角色/场景/道具，全链路方案 §环节5 明确要求 Jellyfish 扩到服装）。

LLM_BINDING_TEMPLATE = Template(
    """
你是短剧制作中台的资产关联助理。任务：判断每个镜头中**实际入画**的人物、场景、道具、服装，
并从候选资产清单中选择对应资产。

【候选资产清单】（asset_id 只能从这里选，绝不允许编造）
$catalog

【镜头列表】
$shots

【判定规则】
1. 只绑定"实际入画/被镜头拍到"的资产；仅在台词里被提到、未入画的不绑定。
2. 场景每镜头最多 1 个；人物、道具、服装可以多个。
3. 剧情里的说法与清单名称不一致时，根据别名和语义判断；清单里没有对应项就不要硬选。
4. 镜头出现了清单之外的实体时，把实体名放进 unmatched_names，并猜测类型
   （character/scene/prop/costume），不要编造 asset_id。
5. confidence 取 0~1：名称或别名完全一致给 0.9 以上；靠语义推断给 0.6~0.9；
   拿不准给 0.5 以下。不要为了凑数给高分。
6. reason 用一句话说明判断依据（引用镜头里的具体依据，不要写"看起来相关"）。
7. shot_id 必须逐字使用【镜头列表】里给出的 ID，不要自己编号。

【输出格式】只输出一个 JSON 对象，不要任何解释或 Markdown：
{
  "shots": [
    {
      "shot_id": "镜头ID",
      "characters": [
        {"asset_id": "资产ID", "confidence": 0.95, "reason": "入画并有动作描写"}
      ],
      "scene": {"asset_id": "资产ID", "confidence": 0.9, "reason": "空间标记明确"},
      "props": [
        {"asset_id": "资产ID", "confidence": 0.85, "reason": "被角色抱在怀里入画"}
      ],
      "costumes": [
        {"asset_id": "资产ID", "confidence": 0.8, "reason": "镜头明确描写该套服装"}
      ],
      "unmatched_names": [
        {"name": "黑袍人", "guessed_type": "character", "evidence": "剧情提到但清单无此人物"}
      ]
    }
  ]
}

集数：$episode_id
""".strip()
)


# ---------------------------------------------------------------------------
# 广告剧情流程：剧情策划（DramaPlan）
# ---------------------------------------------------------------------------
# 配方来源：外部参考项目的最小策划内核（用户 2026-09-25/09-26 拍板移植）。
# 只保留"有主见"的创作规则，**不搬**它的云平台与浏览器端合成。
# 硬约束（用户口径，改这条模板前先确认）：
#   1 开场 3 秒必须有可拍摄的动作冲突；
#   2 卖点不许念参数，必须转成人物欲望 / 冲突 / 破功瞬间；
#   3 结构是起势—升级—反转；
#   4 商品至少出现在一半镜头；
#   5 结尾反转 + 自然购买暗示（不要硬 CTA）；
#   6 每镜必须给出角色、动作、台词、时长、商品是否出现。
# 变量用 $name（string.Template），禁用 str.format——JSON 示例里的花括号会被它误解析。

DRAMA_PLAN_TEMPLATE = Template(
    """
你是顶级短剧编剧 + 广告导演。任务：根据商品信息与本章剧本，产出**一整集的剧情广告方案**。

【商品与导演要求】
$brief_text

【本章剧本】
标题：$chapter_title
$chapter_text

【项目风格】$style_hint
【镜头数】$shot_count 个镜头
【整片时长】约 $duration_hint 秒
【每镜时长只能取】$allowed_durations（整数秒，不要写别的值）

【创作硬要求】
1. **开场 3 秒**必须是**可拍摄的动作冲突**（有人在做一件有张力的事），不要用旁白或环境描写开场。
2. **卖点不许直接念参数**。每个卖点必须转成：人物的欲望、人物之间的冲突，或者"当场破功"的瞬间。
   例：不要写"含有 3% 烟酰胺"，要写"她当着所有人的面把瓶子拍在桌上"。
3. 结构必须是**起势 → 升级 → 反转**：前段建立处境，中段把冲突推高，后段反转收口。
4. **商品至少出现在一半镜头里**（$shot_count 个镜头里至少一半 product_present 为 true），
   且商品出现的方式要自然（被人使用、被抢、被摔、被特写），不要摆拍式展示。
5. 结尾要有**反转 + 自然的购买暗示**，不要生硬的"点击链接购买"。
6. 每个镜头必须给出：**出场角色**（characters 数组）、动作、台词、时长、本镜是否出现商品。
7. 角色名、场景名要具体可复用（"女主"不合格，"林小满"合格）；同一角色在所有镜头里名字必须逐字一致。
8. 台词要短、能演、有信息量；不要写舞台说明混在台词里。

【输出格式】只输出一个 JSON 对象，不要任何解释或 Markdown 代码围栏：
{
  "title": "本集标题",
  "logline": "一句话主线（含商品与冲突）",
  "sellingPoints": ["被剧情化之后的卖点表述"],
  "characters": [
    {"name": "角色名", "profile": {"appearance": "外貌", "identity": "身份"}, "shot_indexes": [1, 2]}
  ],
  "scenes": [
    {"name": "场景名", "profile": {"era_location": "时代地点", "spatial_structure": "空间结构"}, "shot_indexes": [1]}
  ],
  "product": {
    "name": "商品名",
    "description": "商品外观描述（必须来自上面的商品信息，不要改包装与颜色）",
    "profile": {"package": "包装", "logo": "Logo与品牌标识", "selling_points": "关联卖点"},
    "shot_indexes": [1, 3]
  },
  "shots": [
    {
      "index": 1,
      "title": "镜头标题",
      "characters": ["本镜出场角色名"],
      "script_excerpt": "本镜的剧本摘录",
      "description": "画面整体描述",
      "duration": 8,
      "camera_shot": "MS",
      "angle": "EYE_LEVEL",
      "movement": "STATIC",
      "action_beats": ["按时间顺序的动作拍点"],
      "dialogue": [{"speaker": "角色名", "text": "台词", "mode": "DIALOGUE"}],
      "product_present": true
    }
  ],
  "climax": "结尾的反转与购买暗示"
}

补充约束：
- camera_shot 只能取 ECU/CU/MCU/MS/MLS/LS/ELS；angle 只能取 EYE_LEVEL/HIGH_ANGLE/LOW_ANGLE/BIRD_EYE/DUTCH/OVER_SHOULDER；
  movement 只能取 STATIC/PAN/TILT/DOLLY_IN/DOLLY_OUT/TRACK/CRANE/HANDHELD/STEADICAM/ZOOM_IN/ZOOM_OUT。
- dialogue 的 speaker 必须是 characters 里出现过的名字（不要写"旁白"，旁白请用 mode=VOICE_OVER 并给 speaker）。
- 镜头序号从 1 开始、连续、不重复。
""".strip()
)
